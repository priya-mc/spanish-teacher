name: speech-api
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.9
  - pip
  - pip:
    - flask
    - litellm
    - deepgram-sdk
    - python-dotenv
    - loguru
    - requests
    - websockets
    - aiohttp
    - python-multipart
    - prometheus_client
    - gunicorn
    - uvicorn
    - pytest
    - pytest-asyncio
    - flask-cors
    - pyyaml 